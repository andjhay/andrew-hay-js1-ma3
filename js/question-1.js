/* 
  function getRemainder(a,b) {
  return a % b;
  }
*/

const getRemainder = (a, b) => {
  return a % b;
};

console.log(getRemainder(10, 7));

// Or give results like this

const result = getRemainder(10, 7);

console.log(result);

// Or function can be written like this

const getRemainderTest = (a, b) => a % b;

const resultTwo = getRemainderTest(10, 7);

console.log(resultTwo);
